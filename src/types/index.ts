export interface LabNavItem {
  path: string
  icon: string
  title: string
  desc?: string
}

export interface DevTipRow {
  lab: string
  panel: string
  tip: string
}

export interface ListItem {
  id: number
  name: string
}
